name: fastapi_stack

include:
  - path: compose.app.yml
  - path: compose.required.yml
  - path: compose.optional.yml
  - path: compose.monitoring.yml

networks:
  fastapi_net:
    driver: bridge
    name: fastapi_net   # explicit name; avoids directory prefix
